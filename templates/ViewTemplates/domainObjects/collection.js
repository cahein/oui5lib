(function() {
    const primaryKey = oui5lib.mapping.getPrimaryKey("eeeee");
    const listBase = oui5lib.listBase.getObject(primaryKey);

    let eeeeP = ooooo.namespace("do.eeeeP");
    eeeeP = oui5lib.util.extend(eeeeP, listBase);
}());
